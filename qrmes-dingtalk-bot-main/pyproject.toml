[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qrmes-dingtalk-bot"
version = "0.1.0"
description = "DingTalk bot service split from mes_ubuntu"
readme = "README.md"
requires-python = ">=3.10"
