[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qrmes-motor-qc"
version = "0.1.0"
description = "Motor QC service split from mes_ubuntu"
readme = "README.md"
requires-python = ">=3.10"
